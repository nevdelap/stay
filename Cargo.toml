[package]
name = "stay"
version = "0.1.0"
edition = "2021"

[dependencies]
dirs = "6"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"

[[bin]]
name = "stay"
path = "src/main.rs"
